<UserControl x:Class="nanofromage.UserControls.NameUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:nanofromage.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Name="blbName" Text="{Binding CurrentCharacter.Name, Mode=TwoWay}" Grid.Row="0" FontSize="20" Grid.ColumnSpan="2" Grid.Column="0" TextWrapping="WrapWithOverflow" />
        <RadioButton x:Name="male" Content="Garçons" FontSize="24" Grid.Row="1" Grid.Column="1" VerticalContentAlignment="Bottom" HorizontalContentAlignment="Center" />
        <RadioButton x:Name="female" Content="Fille" FontSize="24" Grid.Row="1" Grid.Column="0" VerticalContentAlignment="Bottom" HorizontalContentAlignment="Center" />
    </Grid>
</UserControl>
